global:
tracing:
  client_type: grpc
  endpoint: otel-collector:4317
  insecure: true
  sampling_fraction: 1.0
route:
  receiver: blackhole
  group_by:
  - alertname
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
receivers:
- name: blackhole