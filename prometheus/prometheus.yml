global:
  scrape_interval: 60s 

tracing:
  client_type: grpc
  endpoint: otel-collector:4317
  insecure: true
  sampling_fraction: 1.0

rule_files:
- /etc/prometheus/rules/*.yml

alerting:
  alertmanagers:
  - static_configs:
    - targets:
      - alertmanager:9093
